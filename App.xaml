<Application
    x:Class="KALD_Control.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KALD_Control"
    xmlns:conv="using:KALD_Control.Converters">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Custom Converters -->
            <conv:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
            <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <conv:FrequencyConverter x:Key="FrequencyConverter"/>
            <conv:InterlockStatusToBrushConverter x:Key="InterlockStatusToBrushConverter"/>
            <conv:WaveformButtonTextConverter x:Key="WaveformButtonTextConverter"/>


            <!-- Color Resources -->
            <Color x:Key="AppBackgroundColorHex">#201F1E</Color>
            <Color x:Key="PrimaryAccentColorHex">#0067C0</Color>
            <Color x:Key="SuccessColorHex">#107C10</Color>
            <Color x:Key="WarningColorHex">#F2C811</Color>
            <Color x:Key="ErrorColorHex">#D13438</Color>

            <SolidColorBrush x:Key="AppBackgroundColor" Color="{StaticResource AppBackgroundColorHex}"/>
            <SolidColorBrush x:Key="PrimaryAccentColor" Color="{StaticResource PrimaryAccentColorHex}"/>
            <SolidColorBrush x:Key="SuccessColor" Color="{StaticResource SuccessColorHex}"/>
            <SolidColorBrush x:Key="WarningColor" Color="{StaticResource WarningColorHex}"/>
            <SolidColorBrush x:Key="ErrorColor" Color="{StaticResource ErrorColorHex}"/>

            <!-- Brushes based on Color Resources -->
            <SolidColorBrush x:Key="PrimaryAccentBrush" Color="{StaticResource PrimaryAccentColorHex}"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColorHex}"/>
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColorHex}"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColorHex}"/>

            <!-- Button Styles -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource PrimaryAccentColor}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                <Setter Property="Background" Value="{StaticResource ErrorColor}"/>
            </Style>

            <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
            </Style>

            <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
            </Style>

            <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}"/>
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>

            <!-- Card Styles -->
            <Style x:Key="DataCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            </Style>

            <Style x:Key="ElevatedCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            </Style>

            <Style x:Key="MetricCardStyle" TargetType="Border" BasedOn="{StaticResource ElevatedCardStyle}">
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Padding" Value="12"/>
            </Style>

            <!-- Text Styles -->
            <Style x:Key="HeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>

            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource HeaderStyle}">
                <Setter Property="FontSize" Value="16"/>
            </Style>

            <Style x:Key="SubheaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Setter Property="Margin" Value="0,0,0,4"/>
            </Style>

            <Style x:Key="MetricValueStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="32"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="0,0,4,0"/>
            </Style>

            <Style x:Key="MetricUnitStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="VerticalAlignment" Value="Bottom"/>
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Style>

            <Style x:Key="StatusIndicatorStyle" TargetType="Border">
                <Setter Property="Width" Value="10"/>
                <Setter Property="Height" Value="10"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Margin" Value="0,0,6,0"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
